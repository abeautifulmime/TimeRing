#!usr/bin/env bash

function getListOfFuseLibs ()
{
    if $1 == NULL
    then

        declare -A file_search_paths
        # read the paths
    fi
 # must determine version of bash to ensure that dictionary can be parsed.
    # if $SEARCH_STRING NULL THEN SEARCH DEFAULT DIRECTORIES.
        # Methods.
        # pkgutil --pkgs | list
        # check if package managers exist - homebrew, macports etc and query this.

function checkPythonSitePackages()
{
    return NULL
}
